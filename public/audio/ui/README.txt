Put subtle UI sound effects here (phone-first).

Expected filenames:
- click.wav
- hover_soft.wav
- lock_in.wav
- swipe_whoosh.wav

Notes:
- TV does NOT play `ui.click` / `ui.lock_in` to avoid noise.
- The app has fallbacks to older `/public/audio/*.ogg` files when present.


